#include "market.hpp"

//View market.hpp for the full implementation of the stock market simulator
int main(int argc, char * argv[]) {
    std::ios_base::sync_with_stdio(false);
    Market(argc, argv);
}
